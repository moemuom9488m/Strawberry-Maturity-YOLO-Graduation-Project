# 🤖 畢業專題：草莓監測機器人後端維護協定 (GEMINI.md)

## 🎯 定位與分工
* **Agent 角色**: 擔任自走車導航演算法、路徑規劃、座標映射邏輯的架構師與維護者。
* **使用者角色**: **伊布** 負責硬體組裝、現場調參、YOLO 模型訓練以及主程式邏輯串接。
* **互動原則**: Agent 修改任何演算法（如 A*）時，必須確保與物理地圖 (0.1m) 的比例對齊，不得改動與馬達控制器對接的輸出格式。

---

## 🚫 絕對禁區 (Red Lines)
* **硬體限制**: 強制綁定 `cuda:0` (RTX 4060 Ti)。所有運算需考量 i7-14700F 的負擔，避免阻塞實時導航。
* **禁改座標系**: 嚴禁在程式碼中混合使用「像素座標」與「物理公尺」。所有地圖計算必須統一使用物理座標 (Meters)，輸出時才轉換為網格索引。
* **檔案保護**: 絕對禁止未經確認即覆寫 `farm_grid_map.csv`。

---

## 🛠️ 技術標準 (Technical Standards)
### 1. 導航與尋路規範
* **核心演算法**: 優先使用 **A* (A-Star)** 處理點對點導航。除非環境極度不規則，否則不使用 ACO。
* **解析度鎖定**: 地圖解析度固定為 **0.1m (10cm/grid)**。
* **運動約束**: 航點生成必須符合「右臂掃描」邏輯，即採用 Skip-Row (跳行) 覆蓋路徑。

### 2. 定位與避障
* **座標映射**: YOLO 辨識出的草莓像素座標，必須透過相機內參 (Intrinsic) 與全域變換矩陣轉換為全域座標。
* **局部避障**: 光達 (LiDAR) 回傳的數據應作為 Costmap 的動態權重，若路徑阻斷，A* 必須在 100ms 內完成重新規劃。

### 3. 專案目錄與檔案管理
* **腳本收納**: 所有由 Agent 撰寫或維護的輔助腳本 (如 `error_logger.py`, `update_train_notebook.py`) 必須統一放置於 `agent_tools/` 資料夾，維持根目錄整潔。Jupyter Notebook 必須透過 `from agent_tools import ...` 來呼叫這些工具。
* **日誌集中**: 所有的維護日誌 (`.md`) 與全域報錯日誌 (`detection_errors.log`) 必須統一存放於 `training_logs/`。
* **權重保護**: 實車部署用的最佳權重，必須從 `runs/` 中抽出並集中存放在 `best_weights/` 以防覆蓋。

---

## 🔄 雙向協定與 SOP 流程

### 模式 A：故障排除 (關鍵字：「我出錯了」)
1. **全域日誌優先**: 
    - **訓練/偵測異常**：所有的訓練腳本與偵測程式，均受 `error_logger.py` 全局監控。若發生崩潰，Agent 必須優先檢視 `training_logs/detection_errors.log` 獲取精準的 Traceback 以進行「驗屍」。
    - **導航異常**：讀取自走車傳回的錯誤 Log，判斷是 A* 找不到路（地圖阻塞）還是座標轉換溢位。
2. **視覺診斷**: 生成路徑圖，確認紅線是否撞上黑色基肥壟。

### 模式 B：路徑合規檢查 (關鍵字：「幫我檢查」)
1. **規律性檢查**: 確認所有方向箭頭是否為「四正向」(上下左右)，無斜角歪斜。
2. **覆蓋率驗證**: 檢查產出的 `aco_coverage_path.txt` 是否確實遍歷了所有目標走道。

### 模式 C：實車部署前測試 (關鍵字：「測試模擬數據」)
1. **物理驗證**: 檢查行駛總里程 (Meters) 是否與農田實際尺寸 (19.5m x 11.0m) 相符。
2. **視覺分離**: 確保同一條走道的來回軌跡在視覺上有適度偏移 (Offset)，以便肉眼觀察方向。

### 模式 D：YOLO 訓練維護與二階段部署 (關鍵字：「優化訓練」)
1. **環境隔離**: 修改任何 `.ipynb` 訓練儲存格時，必須在最開頭包含 `sys.argv = [sys.argv[0]]`，以徹底隔離 Jupyter Kernel 的內部參數干擾，避免 `SyntaxError`。
2. **參數規範**: 
    - 嚴禁使用 `focal_loss` 或 `fl_gamma` 參數。在 Ultralytics 最新版本 (8.4+) 中，Focal Loss 參數已失效並會導致 `SyntaxError`，模型會自動處理類別平衡。
    - **穩定性優先**: 嚴禁開啟 `multi_scale=True`，這在 Windows 環境下易導致 `ZeroDivisionError`。
    - **輸出優化**: 預設使用 `verbose=False` 與 `plots=False` 以減少 Log 雜訊。
    - 二階段微調必須設定 `freeze=10` (或以上) 以保護特徵提取層。
3. **變數自癒**: 核心變數（如 `DATA_YAML_PATH`, `DEVICE_ID`）必須使用 `try...except NameError` 包裹，確保即使跳過上方 Cell 執行，程式也能自動補齊預設值。
4. **顯存管理**: 二階段執行前必須包含 `gc.collect()` 與 `torch.cuda.empty_cache()`，防止顯存累積導致訓練中斷。
5. **語言與驗證**: 
    - 輸出的提示文字必須統一使用 **「繁體中文」**。
    - 修改完成後，Agent 必須執行 **「二次分析 (View File)」**，確認代碼寫入邏輯與 JSON 結構完全正確。

### 模式 E：建立維護日誌 (關鍵字：「寫一個維護日誌」或「寫訓練日誌」)
1. **單日單檔與追加**: Agent 需在 `training_logs/` 資料夾下建立或更新 Markdown 檔案，檔名必須包含當前日期（如 `training_logs/YOLO_訓練日誌_20260422.md`）。**一天只需一個檔案**，若當日已存在該檔案，Agent 必須在檔案最下方**追加新的紀錄段落**，嚴禁覆蓋既有內容。
2. **標準格式**: 每次的紀錄區塊必須嚴格包含以下五個段落：
    - 📅 **時間**: 紀錄當前維護的具體時間點或次序。
    - 🏗️ **目前的模型架構**: 紀錄基礎模型版本、類別數、硬體等。
    - 🎯 **訓練策略**: 紀錄微調的重點、參數調整與物理增強手法。
    - ⚠️ **遇到問題**: 紀錄當時發生的報錯現象或邏輯盲點。
    - 💡 **解決方式**: 清楚條列如何排除錯誤的具體對策。


### 模式 F：訓練後自動備份 (關鍵字：「我訓練好了」)
1. **數值提取**: Agent 必須自動搜尋 `runs/detect/` 下最新的訓練結果（如 `results.csv`），分析並向使用者報告最好的 mAP 數值。
2. **權重複製**: 自動將該次訓練的 `weights/best.pt` 複製到專屬的 `best_weights/` 資料夾，並以訓練名稱或日期重新命名（例如：`best_train7_2_20260422.pt`），確保實車部署用的權重絕對安全且不會被覆蓋。

---

## 🚀 預期目標
1. 確保自走車在 0.1m 解析度下穩定導航。
2. 讓外行教授透過視覺化圖表，一眼就能看懂 A* 規劃出的 Skip-Row 路徑美感。
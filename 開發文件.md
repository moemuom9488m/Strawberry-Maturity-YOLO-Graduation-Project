# 🍓 畢業專案計畫書：結合 YOLO 與無人機之果實成熟度監測系統

**專案負責人：** 李銘澄
**團隊成員：** 成冠偉、鍾秉曄、張晏齊
**核心設備：** i7-14700F CPU / NVIDIA GeForce RTX 4060 Ti GPU
**開發環境：** Python (PyTorch / Ultralytics)

---

## 一、 專案概述與目標
本專案旨在解決傳統草莓園監測中人力成本高、辨識準確率不一的問題。我們建立了一個「由天至地」的數位孿生監測系統：利用無人機取得上帝視角地圖，並由搭載 AI 辨識系統的自走車 (AGV) 進行地面精準監測與數據沉澱。

## 二、 系統架構：由天至地的協同作業
系統分為四大核心層級，確保資訊流從空拍影像轉化為決策看板：

1.  **全局建圖層 (Mapping & Pre-processing)**：
    *   **無人機空拍**：使用 DJI Mini 4 Pro 採集農田高空俯視影像。
    *   **SAM2 語義分割**：導入 SAM2 (Segment Anything Model 2) 進行影像自動分割，產出具物理意義 (0.1m 解析度) 的 `farm_grid_map.csv` 矩陣地圖。
2.  **導航策略層 (Planning)**：
    *   **A*演算法**：針對農田網格提供點到點的最短路徑。
    # 備用演算法:ccpp全覆蓋路徑規劃(參考:https://www.guyuehome.com/wap/detail?id=1825484916955906049)
    *   **跳行策略 (Skip-Row)**：規劃符合機械作業限制的交錯巡檢路徑。
3.  **感知執行層 (Perception & Actuation)**：
    *   **YOLOv11 辨識與追蹤**：在移動過程中，即時辨識果實成熟度並進行 ID 追蹤。
    *   **雙軌數據融合**：將「語義資訊 (成熟度)」與「空間資訊 (全域座標)」結合，克服移動造成的座標誤差。
4.  **展示決策層 (Visualization)**：
    *   **Gradio 數位孿生看板**：提供互動式地圖，即時呈現全區成熟度統計。

---

## 三、 技術核心：路徑規劃與座標映射

### 1. A* 演算法與環境適應
在農田網格中，我們使用 A* 演算法確保自走車在巡航時能避開壟位，並在遇到臨時障礙物時，能即時重新規劃路徑。

### 2. 語義與空間的雙軌融合 (Dual-Stream Fusion)
為了解決相機移動導致的座標偏移，我們採用雙軌並行架構：
*   **語義軌道 (Semantics Stream)**：YOLOv11 負責辨識成熟度級別並給予 ID。
*   **空間軌道 (Spatial Stream)**：透過單應性矩陣與機器人位姿同步，將像素座標「定格」在全域物理地圖上。
*   **結果合成**：兩者合成後，沉澱為地圖上的固定數據點，避免重複計算。

---

## 四、 預期執行流程與產出
1.  **無人機掃描建圖**：產出數位孿生基礎地圖。
2.  **A*路徑規劃**：自動生成巡航軌跡。
3.  **自走車實地監測**：執行雙軌融合偵測，產出 `.txt` 座標紀錄檔。
4.  **數據展示**：透過 Gradio 呈現互動式成熟度圓環統計圖，輔助採收決策。
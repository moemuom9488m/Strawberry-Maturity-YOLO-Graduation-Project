

# 🍓 畢業專案計畫書：結合 YOLO 與無人機之果實成熟度監測系統

**專案負責人：** 李銘澄
**團隊成員：** 成冠偉、鍾秉曄、張晏齊
**核心設備：** i7-14700F CPU / NVIDIA GeForce RTX 4060 Ti GPU
**開發環境：** Python

---

## 一、 專案概述與目標
本專案旨在解決傳統草莓園監測中人力成本高、辨識準確率不一的問題。我們建立了一個「由上而下」的數位孿生監測系統：利用無人機取得上帝視角地圖，並由搭載 AI 辨識系統與機械臂的自走車 (AGV) 進行地面精準作業。

## 二、 系統架構：由天至地的協同作業
系統分為三大核心層級，確保資訊流從空拍影像轉化為機械臂的物理動作：

1.  **全局建圖層 (Mapping)**：
    * 使用 **DJI Mini 4 Pro** 無人機採集農田俯視影像。
    * 導入 **SAM2 (Segment Anything Model 2)** 進行影像分割，將農田轉化為具有物理意義 (0.1m 解析度) 的 `farm_grid_map.csv` 矩陣地圖。
2.  **導航策略層 (Planning)**：
    * **跳行策略 (Skip-Row)**：針對自走車「右側機械臂」的硬體限制，規劃出 `UP -> DOWN` 的交錯路徑，確保掃描無死角。
    * **A* 搜尋演算法**：針對方正農田提供毫秒級的最短路徑計算與 100% 決定性的最佳路徑。
3.  **感知執行層 (Perception & Actuation)**：
    * **光達 (LiDAR)**：利用激光雷達進行即時定位 (Localization) 與局部避障，校正泥地打滑造成的位移誤差。
    * **YOLOv11 辨識**：在自走車移動時，偵測果實成熟度並進行全域座標標記。

---

## 三、 技術核心：路徑規劃與座標映射

### 1. A* 演算法與環境適應
在方正的農田網格中，我們使用 A* 演算法搭配曼哈頓距離啟發函數：
$$f(n) = g(n) + h(n)$$
這確保了自走車在巡航時能走絕對直線，並在遇到臨時障礙物（如農具、雜物）時，能即時重新規劃避障路徑。

### 2. 視覺感知與空間轉換
為了將影像中的草莓映射回地圖，我們建立了一套數學轉換管線：
* **影像辨識**：YOLOv11 採用 **640x640** 解析度進行訓練與推論，在確保成熟度辨識質感的同時，優化 RTX 4060 Ti 的運算負擔。
* **物理轉換**：透過相機內參 (Intrinsic) 與外參矩陣，將像素座標轉換為相對於地圖原點的物理公尺座標。
* **採集規範**：鏡頭需保持 **50cm-80cm** 的工作距離，以獲得最佳辨識質感。


## 四、 預期執行流程與產出
1.  **無人機掃描平面圖**：於25m高空以垂直俯視方式對草莓園進行空拍，以生成數位孿生地圖。
2.  **A*路徑規劃**：產出座標清單供馬達控制器執行。
3.  **自走車實地監測**：自走車沿路執行 YOLO 辨識，並即時更新果實成熟度地圖。
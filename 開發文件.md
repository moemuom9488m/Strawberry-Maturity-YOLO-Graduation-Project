# 🍓 畢業專案計畫書：結合 YOLO 與雙側相機 CCPP 之草莓成熟度監測系統

**專案負責人：** 李銘澄
**團隊成員：** 成冠偉、鍾秉曄、張晏齊
     
### 👥 團隊分工與工作職掌
* **李銘澄**：專案架構規劃、YOLOv11 模型微調與實驗、運用 Google Labs 的 Flow 搭配 Roboflow 資料集照片與提示詞生成 AI 模擬巡檢影片以驗證模型效能
* **成冠偉**：2D/3D 地圖巡檢軌跡模擬、專題簡報排版與視覺美編
* **張晏齊**：專題海報製作、Strawberry 原始資料集整理與標註校對
* **鍾秉曄**：國內外參考文獻蒐集研讀、CCPP (全覆蓋路徑規劃) 演算法與避障研究

**核心設備：** i7-14700F CPU / NVIDIA GeForce RTX 4060 Ti GPU (強制綁定 `cuda:0`)
**開發環境：** Anaconda `pytorch` (`C:\ProgramData\anaconda3\envs\pytorch\python.exe`)

---

## 一、 專案概述與目標
本專案旨在解決傳統草莓園監測中人力成本高、辨識準確率不一的問題。我們建立了一個「空地協同」的數位孿生監測系統：利用高空航測空拍取得全局地圖，並由搭載 **雙側相機側拍巡檢系統** 的地面自走車 (AGV) 沿著 **CCPP 全覆蓋路徑** 進行地面精準側拍監測與數據沉澱，最後產出高精度的草莓成熟度數位孿生看板。

---

## 二、 系統架構：空地協同雙側巡檢作業
系統分為四大核心層級，確保資訊流從航測地圖與雙側側拍轉化為精準決策：

1. **全局建圖層 (Mapping & Pre-processing)**：
   * **航測掃描**：使用空拍取得農田高空俯視影像。
   * **HSV 自適應色彩分割與骨架提取**：引進輕量高效的 HSV 色彩空間分割與 CLAHE 直方圖均衡化，有效消除戶外強光與樹葉陰影干擾。接著透過形態學閉運算（Morphology Close）填補壟面空隙並濾除雜草等隨機噪點，最後運用骨架化（Skeletonization）演算法精準提取出田壟與溝渠的物理中心線，以 0.1m 解析度寫入 `farm_grid_map.csv`。此方案相較於重型深度學習模型（如 SAM2）具備毫秒級超低延遲，且完全釋放顯存給 YOLOv11 獨佔，保證了實時避障與導航的流暢度。
2. **導航策略層 (Planning)**：
   * **CCPP 全覆蓋路徑規劃**：針對分割出的溝渠，自動生成 sequential zigzag (無跳行、依序遍歷每一行) 的完整覆蓋巡航路徑，並以 **CCPP 避障規劃器** 進行相鄰走道間的避障與銜接規劃。
3. **感知執行層 (Perception & Actuation)**：
   * **車載雙側側拍系統**：自走車左右兩側各安裝一台相機進行側拍，行駛於溝渠時能同時掃描兩側田壟。
   * **YOLOv11 辨識與追蹤**：即時辨識草莓成熟度並進行 ID 追蹤。
   * **雙側動態局部投影模型**：拋棄高空單應性變換，改以「車載即時 Pose + 橫向偏移量 $W_{\text{offset}}= 0.2\text{m}$ + 測距深度 $d$」動態還原草莓全域物理座標。
   * **DBSCAN 空間聚類去重**：對多個訊框重複偵測到的草莓，運用 DBSCAN 演算法，以 $10\text{cm}$ 歐氏距離作為鄰域半徑 ($\epsilon = 0.1\text{m}$)，進行點雲空間聚類與去重，準確統計真實數量並防止重覆計數。
4. **展示決策層 (Visualization)**：
   * **數位孿生熱力圖**：提供互動式地圖，結合成熟度點雲與決策熱力圖，輔助農友採收決策。

---

## 三、 技術核心：路徑規劃與座標映射

### 1. CCPP 雙側相機全覆蓋路徑規劃 (CCPP Zigzag Coverage)
自走車行駛於溝渠 (Trench Centerlines, $x = k \times 1.2 + 0.95\text{m}$)，兩側鏡頭各負責掃描左右兩旁的田壟。路徑規劃器依序將每一條溝渠加入巡檢佇列，利用 CCPP 規劃器規劃出極為平滑、符合實車物理轉彎約束的 zigzag 全覆蓋巡檢路徑。

### 2. 雙側動態局部投影與 DBSCAN 空間融合 (Bilateral Local Projection & DBSCAN Fusion)
自走車在 CCPP 路徑點 $(X_v, Y_v)$ 以朝向角 $\theta$ 行駛時，雙側相機動態側拍，投影公式如下：
* **左側相機側拍投影** (向行進方向左側偏移 $W_{\text{offset}}$ 及探測距離 $d_L$):
  $$X_g = X_v - (W_{\text{offset}} + d_L) \sin\theta$$
  $$Y_g = Y_v + (W_{\text{offset}} + d_L) \cos\theta$$
* **右側相機側拍投影** (向行進方向右側偏移 $W_{\text{offset}}$ 及探測距離 $d_R$):
  $$X_g = X_v + (W_{\text{offset}} + d_R) \sin\theta$$
  $$Y_g = Y_v - (W_{\text{offset}} + d_R) \cos\theta$$

對所有還原點，運用 DBSCAN 演算法以 10cm 歐氏距離進行空間聚類融合 ($\epsilon = 0.1\text{m}$, $\text{MinPts} = 1$)：
$$D = \sqrt{(X_{\text{new}} - X_{\text{exist}})^2 + (Y_{\text{new}} - Y_{\text{exist}})^2} \le 0.1\text{m}$$
落於同一聚類簇 (Cluster) 的特徵點將被視為同一顆草莓，並取其座標與成熟度均值；獨立點則視為新草莓，藉此精準計算總產量數量。

---

## 四、 預期執行流程與產出
1. **全局建圖**：產出 0.1m 解析度之農田網格地圖 `farm_grid_map.csv`。
2. **CCPP 軌跡生成**：自動生成無跳行的 zigzag 全覆蓋路徑點 `coverage_path`。
3. **自走車側拍巡航**：執行 YOLOv11 辨識與動態投影，進行點雲去重。
4. **決策地圖展示**：輸出三維數位孿生點雲與成熟度熱力圖 (KDE)，引導精準採收。

---

## 五、 實驗評估與核心模型抉擇成果 (Experimental Evaluation & Decision)

為了驗證本系統在實際田野環境下的成熟度偵測精準度，我們針對 **YOLOv11 模型規模（S vs M）**、**特化層架構優化（Baseline vs P2-CBAM）** 以及 **輸入影像解析度（640px vs 800px）** 進行了控制變因的交叉對照實驗。

### 1. 簡報與學術精選對照表 (聚焦 4 組核心決策組別)

在 10 組對照組中，我們提煉了 4 組最具故事性與工程抉擇意義的關鍵模型：

| 評估模型名稱 (YOLO Model) | 輸入影像尺寸 (Resolution) | 最佳精度 Best mAP@0.5 | 綜合精度 Best mAP@0.5:0.95 | 訓練耗時 (Time Cost) |
| :--- | :---: | :---: | :---: | :---: |
| 🏆 **exp2b_1d_img800** (P2-CBAM) | 800 x 800 px | **0.8749** | 0.6258 | 86.8 hr |
| 🥈 **exp1a_yolo11s_baseline** (S基準) | 640 x 640 px | **0.8699** | 0.6284 | 63.9 hr |
| 🥉 **exp2b_1a_img800** (S基準) | 800 x 800 px | **0.8692** | **0.6438** | 65.6 hr |
| ❌ **exp1b_yolo11m_baseline** (M基準) | 640 x 640 px | **0.8648** | 0.4389 | 89.8 hr |

*完整 10 組對比實驗與學習曲線分析詳見：[yolo_experiments_comparison.md](evaluation_results/yolo_experiments_comparison.md)*

### 2. 多維度 Tradeoff (權衡) 分析與決策結論

#### 💡 結論一：模型規模權衡 (YOLO11s vs YOLO11m)
*   **物理發現**：YOLO11s (S 規模，參數約 9.4M) 的 mAP50 達到 **0.8699**，反而領先了參數容量大一倍的 YOLO11m (M 規模，mAP50: **0.8648**)，且訓練時間縮短了將近 26 小時。
*   **決策**：這證明草莓田間成熟度特徵集中，S 規模的容量已達飽和擬合。為了維持車載嵌入式端的高實時推論幀率 (FPS) 並降低硬體功耗，**我們決定鎖定 YOLO11s 作為自走車的核心部署架構**。

#### 💡 結論二：優化架構與 Grad-CAM 物理意義 (Baseline vs P2-CBAM)
*   **物理發現**：加入 P2 微小頭與 CBAM 注意力機制的 `exp2b_1d_img800` (mAP50: **0.8749**) 達到了精準度最高峰。更重要的是，透過 **Grad-CAM 特徵熱力圖** 對比：
    *   **Baseline 模型**：注意力熱力圖較為渙散，容易被反光葉片、雜草與陰影等背景雜訊干擾。
    *   **P2-CBAM 模型**：注意力高度聚焦於草莓本體及果蒂周圍變色區域，背景雜訊幾乎完全被抑制。
*   **決策**：這提供了強力的物理可解釋性，證實優化後的架構能顯著引導模型「看對地方」，在自然強光陰影干擾下具備極佳的魯棒性。

#### 💡 結論三：輸入影像尺寸的邊際效應 (640px vs 800px)
*   **物理發現**：將解析度提升至 800px 時，`Best mAP50-95` 飆升到了全場最高的 **0.6438**，大幅提升了對微小、交界處特徵的捕捉能力；但若進一步盲目拉升至 1024px，運算延遲將大幅增加且容易因為細微葉脈噪點而產生過擬合。
*   **決策**：在實車部署中，**800px** 是精度與計算延遲的最佳黃金平衡點。

### 3. 簡報專用視覺化決策圖表
我們特別為此決策過程繪製了高質感的雙 Y 軸對比圖，已整理在：[ppt_model_selection_comparison.png](evaluation_results/ppt_model_selection_comparison.png)

![簡報專用模型選擇與 Tradeoff 對比圖](evaluation_results/ppt_model_selection_comparison.png)

---

## 六、 創新模組差異解析 (Small-Detect Head w/ RFCBAM)

Q: 那這樣他的輸出會跟原本的 yolo 有差嗎？

答案是：**會有很明顯的差別！** 不論是在「模型輸出的特徵圖結構」**，還是**「最終預測出來的結果」，加了 `RFCBAM` 的小目標檢測頭（Small-Detect Head）都跟原本的 YOLO 有很大的不同。我們可以從以下兩個層面來看它們的差異：

### 1. 特徵圖（Feature Map）質量的差異：雜訊變少、目標變明顯

原本的 YOLO 檢測頭在輸出前，特徵圖上除了草莓之外，可能還殘留了很多樹枝、反光葉片、陰影等雜訊的權重數值。這會導致模型在最後判斷時猶豫不決，或是把綠葉誤認成青草莓。

而加了 `RFCBAM` 後，特徵圖在送進最終的預測層（輸出 Bounding Box 和類別）之前，會經過高強度的篩選：

* **空間上（Spatial）**：背景雜訊的像素權重會被強行壓低（趨近於 0），而草莓所在的像素區域會被強烈放大。
* **通道上（Channel）**：對偵測草莓沒幫助的特徵通道會被過濾，只留下對草莓形狀、邊緣最敏感的通道。

因此，它的**輸出特徵圖會變得非常「乾淨」**，目標物在特徵圖上會像發光一樣清晰。

---

### 2. 最終預測結果（Prediction Output）的差異

因為多了這個結合 `RFCBAM` 的第四個檢測頭，最終的預測結果會帶來以下三個直接的改變：

#### ❶ 突破極限，能抓到「更小」的物體

原本 YOLO11 最小的檢測頭是處理 $80 \times 80$ 的特徵圖（針對 $640 \times 640$ 的輸入），專門抓大約 $8 \times 8$ 像素以上的物體。
這篇論文拉出來的第四個頭（Small-Detect Head）處理的是更高解析度的特徵圖（例如 $160 \times 160$），再加上 `RFCBAM` 鎖定感受野，這讓模型**有能力捕捉到畫面上只有幾個像素點（極其微小、深藏在陰影處）的草莓**。原本 YOLO 會直接漏判（偵測不到）的極小物體，現在都能被框出來。

#### ❷ 預測框（Bounding Box）更精準、不抖動

傳統 YOLO 在面對模糊、小目標時，預測出來的綠色或紅色定位框經常會「忽大忽小」或「瘋狂抖動」，因為邊緣特徵不夠明確。
`RFCBAM` 內部的 **RFA（感受野注意力）** 會動態去貼合草莓的實際邊緣。這使得最後輸出定位框時，**回歸（Regression）的精準度非常高**，框會緊緊貼著草莓的邊緣，不會過大或過小。

#### ❸ 信心分數（Confidence Score）顯著提升，誤判率降低

在雜亂背景下（例如生長在茂密葉片間的草莓），原本的 YOLO 可能只會給出 0.3 或 0.4 的信心分數，甚至因為低於門檻而不顯示。
而這個改良後的輸出，因為經過通道與空間注意力的雙重確認，模型會非常肯定「那裡幾顆就是草莓」，輸出的**信心分數通常會顯著提高**（例如從 0.4 飆升到 0.85），同時把背景雜訊誤判成草莓的機率（FP, False Positives）也會大幅下降。

---

### 💡 總結你在報告時可以強調的重點：

原本 YOLO 的輸出是「通用的」**，在大中小目標流於平均；而這個 `Small-Detect w/ RFCBAM` 的輸出則是**「針對極小目標高度客製化的」。它讓模型在最後關頭「看清目標、集中注意力」，進而交出高精準度、低漏檢率的終期成績單。
